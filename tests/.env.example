# Copy this to .env and set your site URL.
# Credentials are NOT stored here — a temporary admin account is
# created via SSH before each test run and deleted afterwards.
WP_BASE_URL=https://your-site.com
